

/*var respuesta = prompt("Indica un día de la semana");
var respuesta*/

/*Recopilando alphabeto*/
var alphabet= [];
for (var i=65; i<91; i++){
  var letter= String
  .fromCharCode (i);
  alphabet.push (letter);
}

console.log (alphabet);

/*
function fillArray (asciiCode) {
  var alphabet= [];
  for (var i=0; i<26 ; i++){
    var letter= String.fromCharCode (i+asciiCode);
    alphabet.push (letter);
}
return alphabet;
}

fillArray(65)

console.log();
*/
